"""events-curator: AI-curated recurring web searches.

A UI-agnostic pipeline — expand → search → merge → dedup → store → rank — with
per-saved-query personalization. Upcoming events are the flagship example, but
the pipeline is domain-agnostic. See docs/architecture.md.
"""

from __future__ import annotations

__version__ = "0.0.0"
